# Runtime dependencies
# Most pillars use Python stdlib only.
# Pillar 1 (agent_runner.py) requires anthropic for the LLM-facing layer.
# If you only run the deterministic tests, you don't need this — the
# try/except ImportError pattern in each pillar's LLM wrapper means
# tests pass without it.
anthropic>=0.40.0  ; extra == "llm"

# Development / testing
pytest>=7.4.0
